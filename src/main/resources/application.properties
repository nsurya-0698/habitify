spring.application.name=Habit-tracker

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/HabitTrackerdb
spring.datasource.username=postgres
spring.datasource.password=0698

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true     
spring.jpa.properties.hibernate.format_sql=true  # Formats SQL for readability
spring.jpa.show-sql=true

# Connection Pool Settings (Optional but recommended)
spring.datasource.initialization-mode=always
spring.datasource.driver-class-name=org.postgresql.Driver